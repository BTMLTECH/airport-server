<!DOCTYPE html>
<html lang="en">

<!-- <head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Booking Report - <%= companyName %></title>
  <style>
    body { font-family: Arial, sans-serif; background-color: #f4f8fb; color: #003366; margin: 0; padding: 20px; }
    .email-wrapper { max-width: 700px; margin: 0 auto; background-color: #ffffff; border-radius: 8px; box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1); overflow: hidden; }
    .email-header { background-color: #003366; color: #ffffff; padding: 15px; text-align: center; font-size: 20px; font-weight: bold; }
    .email-body { padding: 20px; }
    table { width: 100%; border-collapse: collapse; margin: 0 auto; font-size: 14px; }
    th, td { border: 1px solid #ccc; padding: 10px; text-align: left; vertical-align: top; }
    th { background-color: #e0f0ff; font-weight: bold; color: #003366; width: 30%; }
    tr:nth-child(even) { background-color: #f9f9f9; }
    ul { margin: 0; padding-left: 20px; }
    .email-footer { background-color: #e0f0ff; padding: 15px; text-align: center; font-size: 13px; color: #003366; }
  </style>
</head> -->

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Booking Report - <%= companyName %></title>

<style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f8fb; /* body background */
      color: #003366;
      margin: 0;
      padding: 20px;
    }

    .email-wrapper {
      max-width: 700px;
      margin: 0 auto;
      background-color: #ffffff;
      border-radius: 8px;
      border: 1px solid #ffd580; /* warm border */
      box-shadow: 0 4px 10px rgba(0,0,0,0.05);
      overflow: hidden;
    }

    /* Logo Section */
    .logo-container {
      text-align: center;
      padding: 20px;
      background-color: #fff7e6; /* light warm background */
      border-bottom: 1px solid #ffd580;
    }

    .logo-container img {
      max-width: 80px;  
      height: auto;
    }

    .email-header {
      background-color: #ffa30f; /* brand orange */
      color: #ffffff;
      padding: 15px;
      text-align: center;
      font-size: 20px;
      font-weight: bold;
    }

    .email-body {
      padding: 20px;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      margin: 0 auto;
      font-size: 14px;
    }

    th, td {
      border: 1px solid #ffd580; /* warm border */
      padding: 10px;
      text-align: left;
      vertical-align: top;
    }

    th {
      background-color: #fff7e6; /* light warm background */
      font-weight: bold;
      color: #003366;
      width: 30%;
    }

    tr:nth-child(even) {
      background-color: #f4f8fb; /* even row background */
    }

    .email-footer {
      background-color: #fff7e6; /* light warm footer */
      padding: 15px;
      text-align: center;
      font-size: 13px;
      color: #003366;
      border-top: 1px solid #ffd580;
    }
</style>

</head>

<body>
  <div class="email-wrapper">
     <!-- LOGO -->
    <div class="logo-container">
      <img src="<%= logo %>" alt="<%= companyName %> Logo" />
    </div>

    <div class="email-header">
      Booking Report - <%= companyName %>
    </div>

    <div class="email-body">
      <p>Here are the booking details submitted by the client:</p>
      <table>
        <tr><th>Full Name</th><td><%= fullName %></td></tr>
        <tr><th>Email</th><td><%= email %></td></tr>
        <tr><th>Phone</th><td><%= phone %></td></tr>

        <tr>
          <th>Service(s)</th>
          <td>
            <% if (selectedServicesDetails && selectedServicesDetails.length) { %>

              <% const onlineServices = selectedServicesDetails.filter(s => s.serviceType === 'online'); %>
              <% if (onlineServices.length) { %>
                <strong>Online Services Paid For:</strong>
                <ul>
                  <% onlineServices.forEach(service => { %>
                    <li>
                      <%= service.label %> - 
                      <%= currency === "NGN" ? `₦${service.price?.toLocaleString()}` : `$${service.dollar?.toFixed(2)}` %>
                      <% if (service.selectedFlight) { %> (Flight: <%= service.selectedFlight %>) <% } %>
                    </li>
                  <% }) %>
                </ul>
              <% } %>

              <% const offlineServices = selectedServicesDetails.filter(s => s.serviceType === 'offline'); %>
              <% if (offlineServices.length) { %>
                <strong>Offline Services to be arranged:</strong>
                <ul>
                  <% offlineServices.forEach(service => { %>
                    <li>
                      <%= service.label %>
                      <% if (service.options?.length) { %>
                        <ul>
                          <% service.options.forEach(option => { %>
                            <li>
                              <%= option.type %> - 
                              <%= currency === "NGN" ? option.priceRange : option.priceRangeUSD %>
                            </li>
                          <% }) %>
                        </ul>
                      <% } %>
                    </li>
                  <% }) %>
                </ul>
              <% } %>

            <% } else { %>
              <%= services.join(", ") %>
            <% } %>
          </td>
        </tr>

        <tr><th>Flight Date</th><td><%= flightDate %></td></tr>
        <tr><th>Flight Time</th><td><%= flightTime %></td></tr>
        <tr><th>Flight Number</th><td><%= flightNumber %></td></tr>
        <tr><th>Arrival City</th><td><%= arrivalCity %></td></tr>
        <tr><th>Passengers</th><td><%= passengers %></td></tr>

        <% if (specialRequests) { %>
          <tr><th>Special Requests</th><td><%= specialRequests %></td></tr>
        <% } %>

        <% if (returnService) { %>
          <tr>
            <th>Return Service</th>
            <td>
              Yes
              <% if (returnDate) { %> - Date: <%= returnDate %><% } %>
              <% if (returnFlight) { %> - Flight: <%= returnFlight %><% } %>
              <% if (returnNotes) { %> - Notes: <%= returnNotes %><% } %>
            </td>
          </tr>
        <% } %>

        <% if (discountCode) { %>
          <tr><th>Discount Code</th><td><%= discountCode %></td></tr>
        <% } %>

        <% if (referralSource) { %>
          <tr><th>Referral Source</th><td><%= referralSource %></td></tr>
        <% } %>

        <tr>
          <th>Total Price</th>
          <td>
            <%= currency === "NGN" ? `₦${totalPrice.toLocaleString()}` : `$${totalDollarPrice.toFixed(2)}` %>
          </td>
        </tr>

        <tr><th>Booking Type</th><td><%= type.charAt(0).toUpperCase() + type.slice(1) %></td></tr>
        <tr><th>Payment Reference</th><td><%= reference %></td></tr>
      </table>
    </div>

    <div class="email-footer">
      © <%= new Date().getFullYear() %> <%= companyName %>. All rights reserved.
    </div>
  </div>
</body>
</html>
