<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Your Booking Confirmation - <%= companyName %></title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f8fb;
      color: #003366;
      margin: 0;
      padding: 20px;
    }

    .email-wrapper {
      max-width: 700px;
      margin: 0 auto;
      background-color: #ffffff;
      border-radius: 8px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      overflow: hidden;
    }

    .email-header {
      background-color: #003366;
      color: #ffffff;
      padding: 15px;
      text-align: center;
      font-size: 20px;
      font-weight: bold;
    }

    .email-body {
      padding: 20px;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      margin: 0 auto;
      font-size: 14px;
    }

    th,
    td {
      border: 1px solid #ccc;
      padding: 10px;
      text-align: left;
      vertical-align: top;
    }

    th {
      background-color: #e0f0ff;
      font-weight: bold;
      color: #003366;
      width: 30%;
    }

    tr:nth-child(even) {
      background-color: #f9f9f9;
    }

    .service-list {
      list-style-type: none;
      padding-left: 0;
    }

    .service-list li {
      margin-bottom: 6px;
    }

    .sub-service-list {
      list-style-type: circle;
      margin-left: 20px;
      padding-left: 0;
    }
  </style>
</head>

<body>
  <div class="email-wrapper">
    <div class="email-header">
      Your Booking Confirmation - <%= companyName %>
    </div>

    <div class="email-body">
      <p>Hi <%= fullName %>,</p>
      <p>Thank you for booking with <%= companyName %>. Here’s a summary of your booking:</p>

      <table>
        <tr>
          <th>Booking Reference</th>
          <td><%= reference %></td>
        </tr>
        <tr>
          <th>Booking Type</th>
          <td><%= type.charAt(0).toUpperCase() + type.slice(1) %></td>
        </tr>
        <tr>
          <th>Flight Info</th>
          <td>
            Date: <%= flightDate %><br />
            Time: <%= flightTime %><br />
            Flight Number: <%= flightNumber %><br />
            Arrival: <%= arrivalCity %>
          </td>
        </tr>
        <tr>
          <th>Passengers</th>
          <td><%= passengers %></td>
        </tr>

        <% if (selectedServicesDetails && selectedServicesDetails.length > 0) { %>
          <tr>
            <th>Services Booked</th>
            <td>
              <% const onlineServices = selectedServicesDetails.filter(s => s.serviceType === 'online'); %>
              <% if (onlineServices.length) { %>
                <strong>Online Services Paid For:</strong>
                <ul class="service-list">
                  <% onlineServices.forEach(service => { %>
                    <li>
                      <%= service.label %> - 
                      <%= currency === 'NGN' ? `₦${service.price?.toLocaleString()}` : `$${service.dollar?.toFixed(2)}` %>
                      <% if (service.selectedFlight) { %> (Flight: <%= service.selectedFlight %>) <% } %>
                    </li>
                  <% }) %>
                </ul>
              <% } %>

              <% const offlineServices = selectedServicesDetails.filter(s => s.serviceType === 'offline'); %>
              <% if (offlineServices.length) { %>
                <strong>Offline Services to be arranged:</strong>
                <ul class="service-list">
                  <% offlineServices.forEach(service => { %>
                    <li>
                      <%= service.label %>
                      <% if (service.options?.length) { %>
                        <ul class="sub-service-list">
                          <% service.options.forEach(option => { %>
                            <li>
                              <%= option.type %> - 
                              <%= currency === 'NGN' ? option.priceRange : option.priceRangeUSD %>
                            </li>
                          <% }) %>
                        </ul>
                      <% } %>
                    </li>
                  <% }) %>
                </ul>
              <% } %>
            </td>
          </tr>
        <% } %>

        <% if (returnService) { %>
          <tr>
            <th>Return Service</th>
            <td>
              Yes
              <% if (returnDate) { %> - Date: <%= returnDate %><% } %>
              <% if (returnFlight) { %> - Flight: <%= returnFlight %><% } %>
              <% if (returnNotes) { %> - Notes: <%= returnNotes %><% } %>
            </td>
          </tr>
        <% } %>

        <% if (specialRequests) { %>
          <tr>
            <th>Special Requests</th>
            <td><%= specialRequests %></td>
          </tr>
        <% } %>

        <tr>
          <th>Total Price</th>
          <td>
            <%= currency === 'NGN' ? `₦${totalPrice.toLocaleString()}` : `$${totalDollarPrice.toFixed(2)}` %>
          </td>
        </tr>
      </table>

      <p>If you have any questions or need assistance, feel free to contact us at sourcing@btmlimited.net.</p>
      <p>Safe travels!</p>
    </div>

    <div class="email-footer">
      © <%= new Date().getFullYear() %> <%= companyName %>. All rights reserved.
    </div>
  </div>
</body>
</html>
