<!DOCTYPE html>
<html lang="en">
<!-- <head>
    <meta charset="UTF-8" />
    <title><%= companyName %> - Passenger Detail</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f8fb;
            color: #003366;
            margin: 0;
            padding: 20px;
        }

        .email-wrapper {
            max-width: 800px;
            margin: 0 auto;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            overflow: hidden;
        }

        .email-header {
            background-color: #003366;
            color: #fff;
            padding: 15px;
            text-align: center;
            font-size: 20px;
            font-weight: bold;
        }

        .email-body {
            padding: 20px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin: 0 auto;
            font-size: 14px;
        }

        th, td {
            border: 1px solid #ccc;
            padding: 10px;
            text-align: left;
            vertical-align: top;
        }

        th {
            background-color: #e0f0ff;
            font-weight: bold;
            color: #003366;
            width: 35%;
        }

        tr:nth-child(even) {
            background-color: #f9f9f9;
        }

        .comment-box {
            background-color: #f9f9f9;
            border-left: 4px solid #003366;
            margin-top: 15px;
            padding: 10px;
            font-size: 14px;
            line-height: 1.5;
            white-space: pre-wrap;
        }

        .email-footer {
            background-color: #e0f0ff;
            padding: 15px;
            text-align: center;
            font-size: 13px;
            color: #003366;
        }
    </style>
</head> -->

<head>
    <meta charset="UTF-8" />
    <title><%= companyName %> - Passenger Detail</title>

   <style>
    body {
        font-family: Arial, sans-serif;
        background-color: #f4f8fb; /* body background */
        color: #003366;
        margin: 0;
        padding: 20px;
    }

    .email-wrapper {
        max-width: 800px;
        margin: 0 auto;
        background-color: #ffffff;
        border-radius: 8px;
        border: 1px solid #ffd580; /* warm border */
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.05);
        overflow: hidden;
    }

    /* LOGO */
    .logo-container {
        text-align: center;
        padding: 20px;
        background-color: #fff7e6; /* light warm background */
        border-bottom: 1px solid #ffd580;
    }

    .logo-container img {
        max-width: 80px;
        height: auto;
    }

    .email-header {
        background-color: #ffa30f; /* brand orange */
        color: #ffffff;
        padding: 15px;
        text-align: center;
        font-size: 20px;
        font-weight: bold;
    }

    .email-body {
        padding: 20px;
    }

    table {
        width: 100%;
        border-collapse: collapse;
        font-size: 14px;
    }

    th, td {
        border: 1px solid #ffd580;
        padding: 10px;
        text-align: left;
        vertical-align: top;
    }

    th {
        background-color: #fff7e6;
        font-weight: bold;
        color: #003366;
        width: 35%;
    }

    tr:nth-child(even) {
        background-color: #f4f8fb;
    }

    .comment-box {
        background-color: #f4f8fb;
        border-left: 4px solid #ffa30f;
        margin-top: 15px;
        padding: 10px;
        font-size: 14px;
        line-height: 1.5;
        white-space: pre-wrap;
    }

    .email-footer {
        background-color: #fff7e6;
        padding: 15px;
        text-align: center;
        font-size: 13px;
        border-top: 1px solid #ffd580;
        color: #003366;
    }
</style>

</head>
<body>
    <div class="email-wrapper">
                <!-- LOGO -->
    <div class="logo-container">
      <img src="<%= logo %>" alt="<%= companyName %> Logo" />
    </div>
    
        <div class="email-header">Passenger - <%= companyName %></div>
        <div class="email-body">
            <table>
                <tr><th>Passenger Name</th><td><%= passengerName %></td></tr>
                <tr><th>Contact</th><td><%= contact %></td></tr>
                <tr><th>Email</th><td><%= email %></td></tr>

                <% if (btmProtocolOfficerName) { %>
                <tr><th>BTM Protocol Officer</th><td><%= btmProtocolOfficerName %></td></tr>
                <% } %>

                <% if (partnerProtocolOfficerName) { %>
                <tr><th>Partner Protocol Officer</th><td><%= partnerProtocolOfficerName %></td></tr>
                <% } %>

                <% if (partnerProtocolOfficerMobile) { %>
                <tr><th>Partner Protocol Officer Mobile</th><td><%= partnerProtocolOfficerMobile %></td></tr>
                <% } %>

                <tr><th>Badge Verification</th><td><%= badgeVerification %></td></tr>
                <tr><th>Any Issues During Check-in</th><td><%= checkInIssues %></td></tr>

                <% if (checkInIssues === "yes" && checkInComment) { %>
                <tr>
                    <th>Check-in Issue Details</th>
                    <td>
                        <div class="comment-box"><%= checkInComment %></div>
                    </td>
                </tr>
                <% } %>
            </table>
        </div>

        <div class="email-footer">
            © <%= new Date().getFullYear() %> <%= companyName %>. All rights reserved.
        </div>
    </div>
</body>
</html>
