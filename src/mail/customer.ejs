<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <title>
        <%= companyName %> Passenger Detail
    </title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f8fb;
            color: #003366;
            margin: 0;
            padding: 20px;
        }

        .email-wrapper {
            max-width: 800px;
            margin: 0 auto;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            overflow: hidden;
        }

        .email-header {
            background-color: #003366;
            color: #fff;
            padding: 15px;
            text-align: center;
            font-size: 20px;
            font-weight: bold;
        }

        .email-body {
            padding: 20px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin: 0 auto;
            font-size: 14px;
        }

        th,
        td {
            border: 1px solid #ccc;
            padding: 10px;
            text-align: left;
            vertical-align: top;
        }

        th {
            background-color: #e0f0ff;
            font-weight: bold;
            color: #003366;
            width: 35%;
        }

        tr:nth-child(even) {
            background-color: #f9f9f9;
        }

        .comment-box {
            background-color: #f9f9f9;
            border-left: 4px solid #003366;
            margin-top: 15px;
            padding: 10px;
            font-size: 14px;
            line-height: 1.5;
            white-space: pre-wrap;
        }

        .email-footer {
            background-color: #e0f0ff;
            padding: 15px;
            text-align: center;
            font-size: 13px;
            color: #003366;
        }
    </style>
</head>

<body>
    <div class="email-wrapper">
        <div class="email-header">
            Passenger - <%= companyName %>
        </div>
        <div class="email-body">
            <table>
                <tr>
                    <th>Passenger Name</th>
                    <td>
                        <%= passengerName %>
                    </td>
                </tr>
                <tr>
                    <th>Contact</th>
                    <td>
                        <%= contact %>
                    </td>
                </tr>
                <tr>
                    <th>Email</th>
                    <td>
                        <%= email %>
                    </td>
                </tr>
                <tr>
                    <th>Assigned Protocol Officer</th>
                    <td>
                        <%= protocolOfficer %>
                    </td>
                </tr>
                <tr>
                    <th>Badge Verification</th>
                    <td>
                        <%= badgeVerification %>
                    </td>
                </tr>
                <tr>
                    <th>Any Issues During Check-in</th>
                    <td>
                        <%= checkInIssues %>
                    </td>
                </tr>

                <% if (checkInIssues==="yes" && checkInComment) { %>
                    <tr>
                        <th>Check-in Issue Details</th>
                        <td>
                            <div class="comment-box">
                                <%= checkInComment %>
                            </div>
                        </td>
                    </tr>
                    <% } %>
            </table>
        </div>
        <div class="email-footer">
            © <%= new Date().getFullYear() %>
                <%= companyName %>. All rights reserved.
        </div>
    </div>
</body>

</html>